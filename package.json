{
  "private": true,
  "scripts": {
    "compile": "tsc -b",
    "watch": "tsc -b -w",
    "postinstall": "cd client && yarn && cd ../server && yarn && cd .."
  },
  "devDependencies": {
    "@types/jest": "27.0.1",
    "@types/node": "^12.12.0",
    "jest": "27.1.1",
    "prettier": "2.3.2",
    "ts-jest": "27.0.5",
    "typescript": "^4.3.2"
  },
  "dependencies": {}
}
